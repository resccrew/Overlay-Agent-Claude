[package]
name = "companion-state"
version = "0.1.0"
edition = "2021"
description = "Pure logic: Claude Code JSONL transcript lines -> companion state. No GUI deps on purpose, so it builds anywhere."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
